<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{content}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="new-funding" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_money}}</span>
    investment required!
  </p>
  <p>
    <button type="button" class="btn btn-primary" id="submit-tech-blog">Submit Tech Blog</button>
  </p>
</div>
